import setup.setup as setup
import setup.config as config

SCREEN = setup.SCREEN
GFX = setup.GFX
SIZE = config.start_size
CLOCK = setup.CLOCK
FPS = 60
SIZE_OPTIONS = [(640, 360), (960, 540), (1920, 1080), (1280, 720)]

initial_scene = config.initial_scene
